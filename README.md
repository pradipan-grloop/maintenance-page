##Info

1. change the Client-company-logo image within assests/images/ ; but keep the same name = "client-logo.png"
(If the extension is changed then you need to go to app.jsx and change the extension where it's imported)

2. Rest of the dynamic fields are added in .env file. (Example is added)